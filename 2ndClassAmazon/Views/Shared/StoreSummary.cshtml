@{
}
@model Book
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>
            @Model.Title

            <span class="badge bg-info" style="float:right">
                Price: @Model.Price
            </span>
        </h4>
        
    </div>
    <div class="card-text p-1">Author: @Model.Author</div>
    <div class="card-text p-1">Classification/Category: @Model.Classification/@Model.Category</div>
    <div class="card-text p-1">Page Count: @Model.PageCount</div>
    <div class="card-text p-1">Publisher: @Model.Publisher</div>
    <div class="card-text p-1">ISBN: @Model.Isbn</div>
    
    <form method="post" asp-page="/Order">

        <input type="hidden" asp-for="@Model.BookId"/>
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
        <span class="card-text">
            @*I Changed the styling of the purchase button to clear, and highlighted dark while selected*@
            <button type="submit" class="btn btn-outline-dark btn-lg float-end">Purchase</button>
        </span>
    </form>
</div>

<br />



